package toDoList;

public class StudyForAlgebraTask extends Task {
    
   public StudyForAlgebraTask(){
        super();
    }
   public StudyForAlgebraTask(float reqTime){
       super(reqTime);
   }
   public StudyForAlgebraTask(int priority){
       super(priority);
   }   
   public StudyForAlgebraTask(int priority, float reqTime){
       super(priority, reqTime);
   }
  
}
